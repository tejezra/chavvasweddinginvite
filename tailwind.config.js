/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Vintage Indian Pop-Art Bold Palette
        'pop-gold': '#D4AF37',
        'pop-deep-red': '#8B0000',
        'pop-orange': '#FF6B35',
        'pop-teal': '#006666',
        'pop-magenta': '#D60384',
        'pop-saffron': '#FF9F1C',
        'pop-cream': '#FFF8DC',
        'pop-dark': '#1a1a1a',
      },
      fontFamily: {
        'serif': ['Playfair Display', 'Georgia', 'serif'],
        'sans': ['Inter', 'system-ui', 'sans-serif'],
      },
      spacing: {
        '128': '32rem',
        '144': '36rem',
      },
      animation: {
        'float': 'float 6s ease-in-out infinite',
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      },
      keyframes: {
        float: {
          '0%, 100%': { transform: 'translateY(0px)' },
          '50%': { transform: 'translateY(-20px)' },
        }
      }
    },
  },
  plugins: [],
}
